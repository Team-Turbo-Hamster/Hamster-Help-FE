import { makeStyles } from "@mui/styles";

const useStyles = makeStyles((theme) => ({
  button: {
    width: "50%",
    marginTop: "5px",
    marginBottom: "5px",
  },
}));

export default useStyles;
